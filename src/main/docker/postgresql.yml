version: '2'
services:
  xerus-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/xerus/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=xerus
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
